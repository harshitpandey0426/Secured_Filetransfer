It is a secured file transmission code, which encrypts and decrypts chunks of a file using DES3 module, exchanging key with the help of Deffiehelmen key exchange algorithm.

A class is used as a structre to store all necessary information to required for a file transmission
 
To run:

1. First run Server file then give any random port number.

2. Then run Client file and provide it with same port number as that of server.

3.Next give file name on client side, if it  is available it will get downloaded else it will ge
t disconnected

4. Multuple clients can connect to a server at a time

